---
applicability: report-builder skill — cover page sizing, identity, and tagline discipline
read_when: authoring or extending a component fragment (references/components/); during report builds only for judgment the fragment cannot carry — the 1/3 scale rule + tagline craft (Stage 3)
---

## Cover Is Identity, Not Summary

The cover answers "what is this?" at a glance — project name + brief tagline. Nothing more. A reader should identify the project in 3 seconds; longer means the cover is doing the work of page 2.

## Scale rule (the 1/3 rule)

Cover hero text/numbers should sit at roughly **1/3 the scale** of the in-report hero. A cover that competes visually with p2's hero numbers is too loud — the cover is a handshake, not a billboard.

Concrete: if your in-report hero number is 96–116px (page-defining metric on p2/p3), the cover wordmark sits at ~32–48px and the tagline at body size (~16–18px). Not 116px display on the cover and another 116px on page 2.

If your report has no oversized in-report hero (e.g., a brief register with subtle hierarchy), the cover wordmark sits at ~28–36px — never larger than your largest in-report headline.

## Forbidden on cover

- **Paragraph-shaped hero** (3+ lines of prose explaining what the project does) — essay leakage; the cover is not the place to explain
- **2×2 KPI checkbox blocks** — decorative unless the KPIs ARE the report's subject (e.g., earnings deck whose subject IS the quarter's metrics)
- **Marketing-tagline subtitle** ("the bold reimagining of...", "redefining how...") — kills analytical authority
- **Hero number prominent on cover** — only justified if the report's central finding IS the number itself (e.g., "$2.3B revenue" on an earnings deck)
- **Editorial chrome that misleads** (e.g., "Vol. 01 · Issue 03") — only if the publication actually has issues; otherwise it's pretense

## Tagline craft

- **Length**: ≤8 words preferred; 15 max if technical payload genuinely demands it
- **Voice**: active, declarative — no "is the X for Y" essay-phrases
- **Content**: name the *category* of thing the project is (plugin / framework / dataset / analysis / benchmark) + one verb-phrase for what it does
- **Shape**: `"Project · category — single verb-phrase"` or `"Project: <one-line claim>"`

Examples:

| Good | Bad |
|---|---|
| `Hukuhaka-Project-Mapper · plugin — keeps .claude/ docs in sync with code` | `A documentation orchestration system that helps LLMs maintain project context through automated agent pipelines and structured outputs` |
| `ViT — vision transformers, no convolutions, scaled` | `An overview of the vision transformer architecture and its implications for computer vision` |
| `MANUAL vs ROUND_ROBIN — scheduler comparison on Hailo-8` | `A comprehensive analysis of two scheduling strategies in the HailoFlow runtime` |

## What does belong on cover

- Project name (wordmark or text-set logotype)
- Tagline (≤8 words ideally)
- Optional: short metadata strip (version, date, author) at body size — chrome, not content
- Optional: a single small mark / identifier — chrome only

That's it. If you want more, you want page 2.
